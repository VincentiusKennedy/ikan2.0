import 'package:flutter/material.dart';

class RiwayatTransaksiWidget extends StatelessWidget {
  const RiwayatTransaksiWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BELUM ADA DATA'),
    );
  }
}
